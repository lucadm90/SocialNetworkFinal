 <!-- codice ruby per evitare di mettere 200 linee di codice in +
 si usa il partial per richiamare codice dal file header -->
 <!-- per evitare di mettere 200 linee di codice in +
 si usa il partial per richiamare codice dal file header -->
<!--indirizza tutti i file che abbiamo impostato permette di mettere il testo-->

<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title))%> </title>
  <%= render :partial =>"shared/header"%>
</head>
<body>

<%= render :partial =>"shared/top_menu"%>

<div class="container">

  <%flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %>
        <a class="close" data-dismiss="alert">&#215;</a>
      </div>
  <% end %>

  <hr>
  <%= yield %>
  <hr>

  <%= debug(params) if Rails.env.development?%>
</div>

</body>
</html>